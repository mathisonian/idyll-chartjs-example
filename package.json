{
  "name": "reachartjs",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "idyll index.idyll --defaultComponents=components/default --theme github --css styles.css --watch",
    "build": "idyll index.idyll --defaultComponents=components/default --theme github --css styles.css",
    "deploy": "npm run build && gh-pages -d ./build"
  },
  "dependencies": {
    "chart.js": "^1.1.1",
    "d3": "^4.0.0",
    "idyll": "^2.0.0",
    "idyll-d3-component": "^2.0.0",
    "react": "^16.4.1",
    "react-chartjs": "^1.2.0",
    "react-dom": "^16.4.1"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
